services:
  slskd:
    image: slskd/slskd:latest
    container_name: slskd
    ports:
      - "5030:5030" # web UI HTTP
      - "5031:5031" # web UI HTTPS
      - "50300:50300" # listen for incoming connections
    volumes:
      - slskd_data:/app # Persistent storage for slskd configuration and data
      - ./music:/music:rw # Mount your music library, ensure slskd can read/write
    environment:
      - SLSKD_REMOTE_CONFIGURATION=true
      - SLSKD_URL_BASE=/download
      - SLSKD_USERNAME=${SLSKD_USERNAME}
      - SLSKD_PASSWORD=${SLSKD_PASSWORD}
      - SLSKD_SLSK_USERNAME=${SLSKD_SLSK_USERNAME}
      - SLSKD_SLSK_PASSWORD=${SLSKD_SLSK_PASSWORD}
      - SLSKD_DOWNLOADS_DIR=/music
      - SLSKD_SHARED_DIR=/music
    restart: unless-stopped
  navidrome:
    image: deluan/navidrome:latest
    container_name: navidrome
    ports:
      - "4533:4533" # Navidrome web interface port
    volumes:
      - ./navidrome_data:/data # Persistent storage for Navidrome database and config
      - ./music:/music:ro # Mount your music library (read-only for Navidrome)
    environment:
      - ND_SCANSCHEDULE=1h # Scan for new music every hour
      - ND_LOGLEVEL=trace # Set log level
      - ND_ENABLESHARING=true # Enable sharing features
    restart: unless-stopped
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    ports:
      - "8081:80" # Web UI accessible at http://localhost:8080
    volumes:
      - ./music:/srv # FileBrowser will upload directly to your music library
      - ./filebrowser_data:/database # Persistent FileBrowser config
    environment:
      - FB_BASEURL=/upload
    restart: unless-stopped
  nginx:
    image: nginx:latest
    container_name: nginx-proxy
    ports:
      - "8080:8080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - slskd
      - filebrowser
      - request_queue
    restart: unless-stopped
  request_queue:
    build:
      context: ./request_queue
    container_name: request_queue
    ports:
      - "5002:5000"
    volumes:
      - ./request_queue:/app
      - ./request_queue_data:/app/data
      - ./music:/music:rw
    environment:
      - SLSKD_BASE_URL=${SLSKD_BASE_URL:-http://slskd:5030}
      - SLSKD_USERNAME=${SLSKD_USERNAME}
      - SLSKD_PASSWORD=${SLSKD_PASSWORD}
      - MUSIC_DIR=/music
    depends_on:
      - slskd
    restart: unless-stopped
  slskd_mock:
    build:
      context: ./e2e/slskd_mock
    container_name: slskd_mock
    ports:
      - "5035:5030"
    volumes:
      - ./music:/music:rw
      - ./e2e/slskd_mock:/app
    environment:
      - PORT=5030
    restart: unless-stopped
  zrok_navidrome:
    image: openziti/zrok
    restart: unless-stopped
    user: 1000:1000
    volumes:
      - ${HOME}/.zrok:/home/ziggy/.zrok
    environment:
      PFXLOG_NO_JSON: "true"
    command: share reserved "${STREAMING_SERVER_NAME}" --headless --override-endpoint http://navidrome:4533
    depends_on:
      - navidrome

  zrok_file:
    image: openziti/zrok
    restart: unless-stopped
    user: 1000:1000
    volumes:
      - ${HOME}/.zrok:/home/ziggy/.zrok
    environment:
      PFXLOG_NO_JSON: "true"
    command: share reserved "${FILE_SERVER_NAME}" --headless --override-endpoint http://nginx:8080
    depends_on:
      - nginx

volumes:
  slskd_data: